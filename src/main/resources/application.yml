spring:
  application:
    name: Service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/electron?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    username: root
    password: 200309
  servlet:
    multipart:
      enabled: true #启用multipart文件上传
      max-file-size: 500MB #最大单文件大小
      max-request-size: 500MB #最大HTTP请求
  data:
    redis:
      port: 6379
      password:
      host: 127.0.0.1
server:
  port: 8866
mybatis:
  configuration:
    map-underscore-to-camel-case: true #开启自动驼峰命名
  mapper-locations: mapper/*.xml #扫描mapper文件路径
logging:
  level:
    com.ligg.electronservice: debug #SQL日志级别
jwt:
  key: "ligg"